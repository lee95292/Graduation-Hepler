import React, { Component } from "react";
import { Table } from "react-bootstrap";

class InfoTable extends Component {
  render() {
    const { data } = this.props;
    return <Table>nn</Table>;
  }
}

export default InfoTable;
